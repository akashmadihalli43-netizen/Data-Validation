# Data-Validation
It is a Pandas Script The script collects data from a local file using configuration paths, reads main and individual files, and performs validations like record count, duplicate, and null checks using user-defined functions, SQL queries, and string operations. Finally, it maps columns to compare source and target tables.
